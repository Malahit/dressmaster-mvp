# Dressmaster API

Fastify-сервер, который управляет гардеробом, образами и календарём пользователя.

## 🚀 Быстрый старт (локально)
1. Подними Postgres.
   ```bash
   docker compose -f ../infra/docker-compose.yml up -d
   ```
2. Скопируй переменные окружения и задай значения при необходимости.
   ```bash
   cp .env.example .env
   ```
3. Установи зависимости в корне и сгенерируй Prisma Client.
   ```bash
   pnpm install
   pnpm -C api prisma:generate
   pnpm -C api prisma:migrate   # локальные миграции
   ```
4. Запусти dev-сервер.
   ```bash
   pnpm -C api dev
   ```

## Скрипты
- `pnpm -C api test` — Vitest
- `pnpm -C api lint` — ESLint
- `pnpm -C api build` — сборка в `dist/`
- `pnpm -C api start` — прод-старт (перед запуском выполнит `prisma migrate deploy`)
