# Анализ слабых сторон репозитория Dressmaster MVP

## Критические проблемы (P0)

### 1. **Ошибка в Prisma схеме - проект не компилируется**
**Серьезность:** 🔴 Критическая  
**Файл:** `api/prisma/schema.prisma`

**Проблема:**
Модель `Outfit` имеет две обратные связи (`CalendarEntry` и `Feedback`), но не объявляет поля для них.

```prisma
model Outfit {
  // Отсутствуют:
  // calendarEntries CalendarEntry[]
  // feedbacks       Feedback[]
}
```

**Последствия:**
- Prisma Client не генерируется
- API не может быть запущен
- CI/CD pipeline будет падать

**Решение:**
Добавить обратные связи в модель Outfit.

---

### 2. **Hardcoded JWT Secret в production**
**Серьезность:** 🔴 Критическая (Security)  
**Файлы:** 
- `api/src/index.ts:13`
- `api/src/env.ts:6`
- `api/src/plugins/auth.ts:5`

**Проблема:**
```typescript
const JWT_SECRET = process.env.JWT_SECRET || 'PLEASE_SET_JWT_SECRET';
```

JWT секрет имеет небезопасное значение по умолчанию. Если переменная окружения не установлена, приложение будет использовать предсказуемый секрет.

**Последствия:**
- Любой может подделать JWT токены
- Полная компрометация авторизации
- Доступ к данным всех пользователей

**Решение:**
- Удалить значение по умолчанию
- Падать с ошибкой, если JWT_SECRET не установлен
- Добавить проверку в startup

---

### 3. **Отсутствие валидации владельца ресурса**
**Серьезность:** 🔴 Критическая (Security)  
**Файлы:** 
- `api/src/routes/items.ts:28-36`
- `api/src/routes/items.ts:39-42`
- `api/src/routes/calendar.ts:41-44`

**Проблема:**
Endpoint'ы PATCH/DELETE не проверяют, что пользователь владеет ресурсом:

```typescript
app.patch('/items/:id', { preHandler: [app.authenticate] }, async (req: any, reply) => {
  const { id } = req.params as { id: string };
  // Нет проверки userId!
  const updated = await app.prisma.item.update({
    where: { id },
    data: parsed.data
  });
});
```

**Последствия:**
- Пользователь A может редактировать/удалять вещи пользователя B
- Нарушение конфиденциальности данных
- OWASP A01:2021 - Broken Access Control

**Решение:**
Добавить проверку `where: { id, userId: req.user.id }` во все операции изменения/удаления.

---

## Важные проблемы (P1)

### 4. **Слабая валидация паролей**
**Серьезность:** 🟡 Высокая (Security)  
**Файл:** `api/src/routes/auth.ts:7`

**Проблема:**
```typescript
password: z.string().min(6)
```

Минимальная длина пароля 6 символов недостаточна для безопасности.

**Решение:**
- Увеличить до минимум 8-12 символов
- Добавить требования: буквы, цифры, спецсимволы
- Использовать библиотеку проверки сложности пароля

---

### 5. **Отсутствие rate limiting**
**Серьезность:** 🟡 Высокая (Security)  
**Файлы:** `api/src/routes/auth.ts`

**Проблема:**
Нет защиты от brute-force атак на endpoint'ы авторизации.

**Последствия:**
- Атакующий может подбирать пароли
- DDoS атаки
- Высокие затраты на инфраструктуру

**Решение:**
Добавить `@fastify/rate-limit` plugin.

---

### 6. **Отсутствие логирования ошибок**
**Серьезность:** 🟡 Высокая (Operations)  
**Файлы:** Весь API

**Проблема:**
Ошибки просто возвращаются клиенту без логирования:

```typescript
if (!parsed.success) return reply.code(400).send({ error: 'invalid_body' });
```

**Последствия:**
- Невозможно отследить проблемы в production
- Нет данных для debugging
- Нет мониторинга ошибок

**Решение:**
- Добавить централизованное логирование ошибок
- Интегрировать Sentry или аналог
- Логировать все 4xx/5xx ответы

---

### 7. **Отсутствие миграций базы данных**
**Серьезность:** 🟡 Высокая (Operations)  
**Файл:** `api/prisma/` (нет папки migrations)

**Проблема:**
Нет версионирования схемы БД, отсутствуют файлы миграций Prisma.

**Последствия:**
- Невозможно откатить изменения БД
- Проблемы при развертывании
- Несинхронизация схемы между окружениями

**Решение:**
Запустить `prisma migrate dev` для создания миграций.

---

### 8. **Отсутствие обработки ошибок БД**
**Серьесность:** 🟡 Высокая (Reliability)  
**Файлы:** Все route файлы

**Проблема:**
Нет try-catch блоков для операций с БД:

```typescript
const user = await app.prisma.user.findUnique({ where: { email } });
// Что если БД недоступна?
```

**Последствия:**
- Unhandled promise rejection
- Падение сервера
- Утечка информации об ошибках

**Решение:**
Добавить error handling middleware и try-catch блоки.

---

### 9. **Использование `any` типов**
**Серьезность:** 🟡 Средняя (Code Quality)  
**Файлы:** 
- `api/src/index.ts:21`
- `api/src/routes/auth.ts:39`
- `api/src/routes/items.ts:13,18,28,39`
- И многие другие

**Проблема:**
```typescript
app.decorate('authenticate', async (request: any, reply: any) => {
```

Использование `any` отключает проверку типов TypeScript.

**Последствия:**
- Потеря преимуществ TypeScript
- Ошибки в runtime вместо compile-time
- Сложность рефакторинга

**Решение:**
Использовать правильные типы Fastify или создать свои интерфейсы.

---

## Проблемы качества кода (P2)

### 10. **Отсутствие тестов**
**Серьезность:** 🟠 Средняя (Quality)

**Проблема:**
Только 1 тест для health endpoint, нет тестов для:
- Auth (register/login)
- Items CRUD
- Outfits generation
- Calendar
- Authorization checks

**Mobile:** `"test": "echo \"no tests yet\""`

**Последствия:**
- Высокий риск регрессий
- Невозможно безопасно рефакторить
- Баги доходят до production

**Решение:**
Написать интеграционные тесты для критичных флоу.

---

### 11. **Примитивная логика генерации образов**
**Серьезность:** 🟠 Средняя (Product)  
**Файл:** `api/src/services/generator.ts`

**Проблемы:**
- Hardcoded цвета и правила
- Только первый аксессуар используется: `const acc = accessories[0];`
- Примитивная логика температуры (не учитывает сезон вещи)
- Нет учета предыдущих выборов пользователя
- Магические числа без объяснений

```typescript
function filterByTemp(items: Item[], temp?: number) {
  if (temp == null) return items;
  return items.filter((i) => {
    const f = i.formality ?? 3; // Почему formality для температуры?
    if (temp < 10) return f >= 3;
    if (temp > 23) return f <= 3;
    return true;
  });
}
```

**Последствия:**
- Низкое качество рекомендаций
- Пользователи не будут доверять системе
- Не учитываются реальные предпочтения

**Решение:**
- Использовать поле `season` для фильтрации по температуре
- Добавить больше правил сочетаемости
- Рассмотреть использование ML для улучшения рекомендаций

---

### 12. **Отсутствие валидации дат**
**Серьезность:** 🟠 Средняя (Security/UX)  
**Файл:** `api/src/routes/calendar.ts:23`

**Проблема:**
```typescript
date: z.string(), // ISO date
```

Строка не валидируется как дата, можно отправить `"not-a-date"`.

**Последствия:**
- Invalid Date в БД
- Ошибки при запросах календаря
- Плохой UX

**Решение:**
Использовать `z.string().datetime()` или кастомный валидатор.

---

### 13. **CORS настроен на все источники**
**Серьезность:** 🟠 Средняя (Security)  
**Файл:** `api/src/index.ts:17`

**Проблема:**
```typescript
await app.register(cors, { origin: true });
```

Разрешены запросы с любого домена.

**Последствия:**
- Риск CSRF атак
- Любой сайт может делать запросы к API

**Решение:**
Ограничить origin конкретными доменами или использовать динамическую валидацию.

---

### 14. **Отсутствие пагинации**
**Серьезность:** 🟠 Средняя (Performance)  
**Файлы:** 
- `api/src/routes/items.ts:13-15`
- `api/src/routes/outfits.ts:47-48`
- `api/src/routes/calendar.ts:5-18`

**Проблема:**
Все списки возвращаются полностью без limit/offset.

**Последствия:**
- Медленные запросы при большом количестве данных
- Высокое использование памяти
- Плохой UX на мобильном

**Решение:**
Добавить cursor-based или offset-based пагинацию.

---

### 15. **Отсутствие индексов в БД**
**Серьезность:** 🟠 Средняя (Performance)  
**Файл:** `api/prisma/schema.prisma`

**Проблема:**
Нет индексов для часто запрашиваемых полей:
- `Item.userId`
- `Outfit.userId`
- `CalendarEntry.date`

**Последствия:**
- Медленные запросы при росте данных
- Full table scans
- Высокая нагрузка на БД

**Решение:**
Добавить `@@index([userId])` и другие необходимые индексы.

---

### 16. **Уязвимость к SQL Injection через JSON поля**
**Серьезность:** 🟠 Средняя (Security)  
**Файл:** `api/prisma/schema.prisma:47`

**Проблема:**
```prisma
accessoryIds String[] @default([])
```

Массив строк может содержать небезопасные данные, хотя Prisma и защищает от SQL injection.

**Решение:**
Валидация UUID перед сохранением.

---

## Минорные проблемы (P3)

### 17. **Дублирование кода**
**Серьезность:** 🟢 Низкая (Maintainability)

**Проблема:**
- Повторяющаяся логика error handling
- Дублирование authentication setup (в index.ts и plugins/auth.ts)
- Повторяющиеся validation patterns

**Решение:**
Вынести в переиспользуемые утилиты.

---

### 18. **Отсутствие документации API**
**Серьезность:** 🟢 Низкая (DX)

**Проблема:**
Нет OpenAPI/Swagger спецификации, только файл `docs/api-spec.md` (не проверялся).

**Решение:**
Использовать `@fastify/swagger` для автогенерации документации.

---

### 19. **Отсутствие healthcheck для БД**
**Серьезность:** 🟢 Низкая (Operations)  
**Файл:** `api/src/routes/health.ts`

**Проблема:**
Health endpoint не проверяет доступность БД.

**Последствия:**
- Kubernetes/Docker может считать под healthy, даже если БД недоступна

**Решение:**
Добавить `await prisma.$queryRaw\`SELECT 1\`` в health check.

---

### 20. **Слабая типизация схем Zod**
**Серьезность:** 🟢 Низкая (Code Quality)

**Проблема:**
Нет переиспользуемых схем, все inline.

**Решение:**
Вынести схемы в отдельные файлы, использовать для генерации TypeScript типов.

---

### 21. **Использование устаревших версий зависимостей**
**Серьезность:** 🟢 Низкая (Security)

**Проблема:**
Некоторые пакеты могут иметь известные уязвимости.

**Решение:**
Регулярно запускать `npm audit` и обновлять зависимости.

---

### 22. **Отсутствие .editorconfig использования**
**Серьезность:** 🟢 Низкая (Code Style)

**Проблема:**
Файл `.editorconfig` есть, но не все настройки соблюдаются (например, trailing spaces).

**Решение:**
Настроить pre-commit hooks с Husky + lint-staged.

---

### 23. **Пустой seed файл**
**Серьезность:** 🟢 Низкая (DX)  
**Файл:** `api/prisma/seed.ts`

**Проблема:**
```typescript
console.log('Seed: nothing to seed yet.');
```

Нет тестовых данных для локальной разработки.

**Решение:**
Добавить seed с примерами пользователя, вещей и образов.

---

### 24. **Hardcoded значения в UI**
**Серьезность:** 🟢 Низкая (UX)  
**Файл:** `mobile/src/screens/GenerateScreen.tsx:13`

**Проблема:**
```typescript
const results = await generateOutfits('work', 15); // офис + 15°C
```

Тип события и температура захардкожены.

**Решение:**
Добавить UI для выбора.

---

### 25. **Отсутствие обработки ошибок сети в Mobile**
**Серьезность:** 🟢 Низкая (UX)  
**Файлы:** `mobile/src/services/api.ts`

**Проблема:**
Нет retry логики, нет кеширования, нет offline режима.

**Решение:**
Использовать React Query или SWR для лучшей обработки состояния данных.

---

## Архитектурные замечания

### 26. **Отсутствие разделения concerns**
**Серьезность:** 🟢 Низкая (Architecture)

**Проблема:**
- Бизнес-логика в route handlers
- Нет service layer
- Прямой доступ к Prisma из routes

**Решение:**
Ввести слой services для бизнес-логики.

---

### 27. **Отсутствие CI для mobile**
**Серьезность:** 🟢 Низкая (DevOps)

**Проблема:**
`.github/workflows/mobile-ci.yml` существует, но запускается ли он?

**Решение:**
Убедиться, что mobile CI работает и запускает линтер.

---

## Резюме

### Критические (требуют немедленного исправления):
1. ✅ Исправить Prisma схему
2. ✅ Убрать дефолтный JWT_SECRET
3. ✅ Добавить проверку владельца ресурсов

### Важные (до запуска в продакшн):
4. Улучшить валидацию паролей
5. Добавить rate limiting
6. Настроить логирование ошибок
7. Создать миграции БД
8. Добавить обработку ошибок БД
9. Исправить TypeScript типы

### Желательно:
10-27. Улучшения качества, тесты, документация, UX

---

## Оценка зрелости проекта

**Текущее состояние:** 🔴 Pre-alpha / MVP в разработке

**Готовность к продакшн:** ❌ Не готов
- Критические security issues
- Отсутствие тестов
- Ошибка компиляции

**Рекомендации:**
1. Исправить все P0 проблемы (1-3)
2. Исправить большинство P1 проблем (4-9)
3. Написать базовые тесты
4. Провести security audit
5. Только после этого запускать beta

**Время на исправление критических проблем:** 2-4 часа работы
**Время на доведение до production-ready:** 1-2 недели работы
